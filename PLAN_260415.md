# NBAS Project Plan: April 15, 2026 (v0.9.0 -> v1.0.0)

## 0. Bugs: things that have to be fixed
*  **1. The Tonight's Sky Widget** padding-top: 90% works for desktop, but 38% works for mobile
*  **2. The Article Listing Page** has nothing on it, and it's called "Resource Library" for some reason.

## 1. Goal: The "Real-World" Article Stress Test
*   **1.1 Content Load:** Import a full-length article (Text, Images, Tables) to stress-test the v0.9.0 CSS.
*   **1.2 Text Wrapping:** Update `nbas-image` shortcode with `align="left|right"` and `clear` logic.
*   **1.3 Image Buffers:** Implement the "20px Breathing Room" rule in CSS for wrapped images.
*   **1.4 Article Tables:** Design a global `.article-table` class for data-heavy content.
*   **1.5 Image Gallery Widget:** Parameters:
       1. given a directory of images below the article directory, load them
       2. get metadata from the article's index.md file (captions, etc.) and display.  
       3. Different display options (grid, jumble, slideshow, etc.) 
       4. THIS HAS BEEN REQUESTED FROM MEMBERS SO ONCE WE HAVE "REGULAR" ARTICLES SOLVED should be prioritized.
*   **1.6 Social Media:** Is there a way to tag an article so when it goes live it does a "Share" to the NBAS Facebook Page?
       * This wouldn't be for EVERY article, but some of them.
*   **1.7 "Spare" Feature SVGs:** 
       * we need to have a bunch of additionial SVG icons for Feature types so that as we add Features we can use them - about 20?
*   **1.8 "Author" Pages:**  Articles link to them if they exist?
*   **1.9 /article organization:** should we organize articles by /article/(year)? or /article/(year)/(month)?

## 2. Home Page
*   **2.1** Limiting the number of Articles on the page
*   **2.2 Faceted Article Listing pages:**
       * by Feature
       * by Tag
       * ? or is this just some sort of faceted search with one "page" and the content is the search results?
         If so, then we'll have to come up with some sort of internal navigation to work with it.
*   **2.3 Provenance:** Somewhere it should say "Founded 2023" in the top matter.  Another field in the Banner?

## 3. Goal: Navigation & Global UI
*   **3.1 Nav Active States:** Use CSS/Hugo to highlight the "Current Page" in the sidebar (Logo Gold).
       * see 3.5 - might no longer be in the Side bar but a new navigational element
*   **3.2 Home Link from Banner:** Can we link the logo on the banner to the Home Page?dd
*   **3.3 Responsive Polish:** Fine-tune the "Sandwich" transition points for tablet-sized screens.
*   **3.4 Footer Branding:** Finalize the "Clear Skies" sign-off and legal footer in `baseof.html`.
*   **3.5 Better Intra-Site navigation:**
       * Right now Home and Library are at the BOTTOM of the left-hand Nav
       * should there be a horizonal nav bar below the banner but above the two content columns?

## 4. Utility Pages
    **4.1 where do these live?**   Can we just crank them out?

## 5. Hugo:
    **5.1 Member-Only Section:** Is this doable?
    **5.2 Editorial:** List Articles that aren't published yet
       * Articles that need editorial approval before publish?

## 6. TODO List
*   [ ] Audit: Ensure all 90 Library PDFs are rendering with correct MB sizes.
*   [ ] Debug: Monitor SVG Moon "Waning" transition for accuracy.
        * I have psuedo-code that is a solution to the bug
*   [ ] Widget: Add "Expand to Fullscreen" link for the Virtual Sky map. --- OH YES PLEASE!
*   [ ] Refactor: Move remaining small inline styles from `baseof.html` to `custom.css`.
*   [ ] Census and Cleanup:  identify any moldy files and remove

## 7. Musings & Notes
*   **CSS Standards:** Stick to the "Plus Rule" for any future asset URLs.
*   **Color Strategy:** Continue the Hue-Sliding (Electric Gold -> Sunset Orange -> Bronze) to define UI hierarchy.
*   **Content Hygiene:** Every new article should be its own folder (Leaf Bundle) with an `index.md`.
*   **The 1.0 Vision:** We are 0.1 away from a feature-complete launch candidate. Focus on stability.
*   **Store:** I need to get back to finding a place to add as a store
*   **Content:** We need an internal "page" where we can share article ideas
