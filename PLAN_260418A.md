# HANDOVER DOCUMENT: NBAS PROJECT (STABLE V0.12.0)
**Date:** April 18, 2026  
**Status:** Feature-Complete UI / Broken Widget Logic

## 🚨 CRITICAL OPERATING PROTOCOLS (MANDATORY)
1. **ANTI-SYCOPHANCY:** Do not apologize. Do not mirror frustration. Do not invent "reporting to superiors." Respond only to technical data.
2. **RULE #1 (URL CONSTRUCTION):** The LLM systemically truncates URLs. **YOU MUST** split every URL component into separate strings and concatenate them with ` + ` in the code. 
   * *Example:* `const url = "https://foo.com" + "/v1/bar" + "?lat=123" + "&lng=456";`
3. **NO RECOVERY NARRATIVES:** If a bug is identified, state the fix and stop. No explanations of "why" unless asked for a debug log.

---

## 🛠 CURRENT TECHNICAL STATE
* **Engine:** Hugo (Static Site Generator)
* **Search:** Pagefind (Client-side indexing). Trained to search `<body>` but display `.Description` metadata via `excerpt[content]`.
* **CSS:** Centralized in `assets/css/*.css`. 
* **Version Control:** Git tagged at `v0.12.1`. 

---

## 📅 NEXT ARCHITECTURAL STEPS (V0.13.0)
1. **Taxonomy Reorganization:**  - THIS IS DONE AND WORKING
   * Move DSOs (Messier/Caldwell) and Constellations out of generic "Tags" into dedicated Hugo taxonomies.
   * Update Top Nav to show "Articles by: Recent | Series | Tag | Constellation | Catalog | Solar System." --- THIS IS BROKEN
2. **Article Metadata Generator:** - TODO - MAYBE we will get to this today
   * Create a "Private" (ignored by Pagefind/Lists) internal page.
   * Build a JavaScript-based HTML form that generates a clean YAML front-matter block.
   * Include a **"Download index.md"** button to allow members to create perfect Hugo content without Git/Markdown knowledge.

---

## 📂 FILE INVENTORY
* `layouts/partials/head.html`: Metadata training and Pagefind UI initialization.
* `layouts/articles/single.html`: Content signal wrapper (`data-pagefind-body`).
* `assets/css/02-layout.css`: Layout and Search UI compaction.
* `assets/css/cards.css`: Unified Pill/Badge styling.
* `assets/css/navbar.css`: The BROKEN CSS styling
* `hugo.toml`: Site params and versioning.
