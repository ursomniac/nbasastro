# PROJECT NBAS: DEVELOPMENT PLAN (PLAN_260422.md)
**Session Date:** 2026-04-22  
**Target Version:** V0.15.0-DEV  
**Status:** STABILIZED / RECOVERED (V0.14.3 Baseline)
---
## 1. The INTERNAL part of the website:

*   NOT visible to:
    * the general public
    * the internal search engine
    * web crawlers
    * Google search
*   Because the site is static - we don't have the ability (yet) to have member login
*   At the same time we want to encourage member contribution of articles
    * Achieving that requires:
        * good documentation
        * reliable procesess for content create and submission
        * continual refactoring of the tools created corresponding to site changes/updates

## 2. The proposed setup:
*   * Create a /members endpoint
*   * NOT referenced on the site from any navigation (see above)
*   * This should have a Members "home" page with it's own sidebar JUST for this section
*   * From here members should be able to:
        * set up the site locally (complete instructions Windows/Mac)
        * or using an external service, e.g., CloudCannon
            * Are there other options?
        * have instructions for how to start creating article directories
            * in approved spaces in /content/articles - NOT ELSEWHERE
        * have sample templates for .md files for different types of articles
            * and variations showing how to used site features (images, video, galleries, etc.)
        * have a process that allows them to test/preview content, and interate
        * have instructions for submitting content
            * could be through the Github repo
            * can EVEN be "send a ZIP file to Bob" (for now)
     * Easy to comprehend instructions on how to set up Article Metadata
        * Which metadata elements are necessary overall
        * Which are custom for different taxonomies
        * Which are optional
        
## 3. /members/demo

HERE I want to have a series of Articles that demonstrate the site/article features:
     * An article that uses several different ways of handling images
     * An article that shows different ways of doing galleries
     * An article incorporating video
     * An article that has lots of SSO or DSO cross-referencing (the Solar System and Catalog taxonomies)
     * An article that's a good example of an Author page
     * An article that is part of a Series
     * An article whose publish date is in the future
     * An article that should produce a posting to our Facebook group WHEN IT GOES LIVE
         * (how this works is still to be determined) - deferred to just before V1.0 releatse
     * An article with tables, links, and whatever other features Markdown supports
     
The /demo endpoint should also include a listing page - just like the other 
listing pages on the public-facing site but with the Demo articles 
(that way we can also show how things like tagging, Series assignment, SSO/DSO assignment,
etc. all "work" going from the index.md file metadata to "how it shows up on the site".

### 3.1. Rules

1. It is VERY important that we understand the scope of this before we start developing it!
2. We have to work on this PIECEMEAL:
     * Identify ONE new change/addition to make
     * Execute, test, and confirm before moving on
     * Identify what the "next step" should be
     * Frequent incremental version updates, and pushing when we hit milestones
         * THIS IS VERY IMPORTANT because it allows us to easily "fallback" if we hit dead-end issues!

### 3.2. Expected layout (subject to change)
* /members  = "home" page with custom navigation (but including the navbar and footer)
* /members/docs = all the "how to" articles with sample code - list with descriptions
* /members/demo = all the "demo" articles - this endpoint is also a listing page
* /members/templates = all the "fill in the blank" templates
    * this would also include snippets (also "fill in the blank"):
        * sample nbas-image, nbas-video, nbas-slideshow, etc.
* /members/process = checklists for content submission 
    * IDEAL: a way to ID a (new/edited) article directory and handle the push to Github?

## 4. V0.14 CORE BENCHMARK: THE DARK SECTOR (INTERNAL TOOLS)
THIS IS FROM EARLIER NOTES.   Use the above sections as "the vision" and this as "the practical considerations" and ADAPT accordingly.

The goal is to create a "Member-Only" directory that is invisible to the public, search engines, and internal site search.

### A. EXCLUSION PROTOCOLS
*   **[ ] Directory Lockdown:** Initialize `content/members/` with zero internal links in `nav.html`.
*   **[ ] Archetype Automation:** Implement `archetypes/members.md` with:
    *   `_build: list: never` (Prevents appearing in "Recent" lists)
    *   `sitemap: disable: true` (Removes from sitemap.xml)
*   **[ ] SEO Suppression:** 
    *   Update `static/robots.txt` with `Disallow: /members/`.
    *   Hard-code `<meta name="robots" content="noindex, nofollow">` into the members layout.
*   **[ ] Search Exclusion:** Apply `data-pagefind-ignore` to the `/members` layout to strip documentation from the local search index.

### B. INTERNAL DOCUMENTATION DEPLOYMENT
*   **[ ] Local Environment Guide:** "Parity between Windows/Mac" setup guide.
*   **[ ] Build Pipeline Docs:** Instructions for Hugo spin-up and `npx` regeneration.
*   **[ ] Content Creation Rules:** Standard operating procedures for metadata entry to prevent "GIGO" build failures.
*   **[ ] Helper Forms:** Templates for rapid generation of `index.md` files for Newsletters and Constellation profiles.


