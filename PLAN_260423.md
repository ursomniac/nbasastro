# PROJECT NBAS: DEVELOPMENT PLAN (PLAN_260422.md)
**Session Date:** 2026-04-23  
**Target Version:** V0.15.0-DEV  
**Status:** STABILIZED / RECOVERED (V0.14.3 Baseline)
---

# 1. BUGS

## 1.1: Mobile styling not quite right:

* in the Banner above the navbar, on iPhone:

    1. The text bleeds into the page - need to compress it

* in the footer items are extending off the right side

I have screenshots showing this.

# 2. Confirmation of Series

I can't remember how this works:

    1. If I just create a new Series entry in the metadata of an article,
       e.g., "series: ["my new series"], does that just create itself on
       the Series page, and with the article Card showing the "my new series"
       banner?  OR do we have to explicitly register each new Series?

       I know that we had to do some machinations getting "Newsletter" and
       "Annoucement" to show up properly.

# 3. Continued work on /members

For the most part, this comes down to "building pages".   For some of those
documents, I'll need to be able to show "code" (e.g., "how do you put in an image"
with the nbas-image snippet but I don't want Hugo to interpret it as a command.

Is it just a case of wrapping the code in "```" blocks?  If so, then that takes
care of that issue.

# 4. Continued work on Articles

Again this comes down to "building pages".  One additional metadata feature 
that would be a nice-to-have:

* Right now we have date: in the metadata which handles the gateway of "when does
this go live".  FOR THE MOST PART, that's all we'll ever need for that.
In a FEW cases, though (the Constellation Highlight pages), we WILL be revisiting
them over time, and adding more content to them.

* Do we want to add another (optional) metadata field, "revised: YYYY-MM-DD"
to indicate that an article was first published on the date:  date but has been 
republished on the later revised: date?   It's fine if this idea is too complicated,
and we can just edit the "date:" field (and of course what happens if you revise 
an article more than one times, so maybe it IS too complicated - it's just a simple
website after all).

# 5. Social Media

## 5.1. Facebook integration
WITHOUT MAKING CHANGES YET (this is for info gathering),  what do we need to do to:

1. Set an article up for OPTIONALLY posting it to our Facebook Group?
    * What metadata do we need to add?
    * How do we make sure that things don't get posted more than once?
      Since we can't update the index.md file on publish, I'm not sure how to 
      solve this.
    * What FB-related things do we need?   Ideally, the posting would have:
        1. an image
        2. the title of the article
        3. a short blurb - right now there IS a "description: " field in the metadata
           but as far as I can tell it's not used on the site.  If that's true,
           then maybe make it "social-description:"?
        4. Anything else I'm forgetting

We can't "do" the FB integration until the site goes live (i.e., after V1.0.0 is 
released, but it'd be ready-to-go as V1.0.1 or something).

I'm expecting this would be some kind of Boolean field "facebook: true" (default false, 
if missing) but there may be another way to do it.

2. What about the ubiquitous "Share" links you see on sites.  It would also be a nice
to have on Articles to make it easy for the end user to promote articles on other
social media platforms, mail, text, etc. where possible.

## 5.2. Other Social Media Sites
We don't have any others yet.  But if we were to add others, how does this alter
the §5.1 work?   
