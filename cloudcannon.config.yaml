collections_config:
  newsflash:
    path: content/newsflash
    output: false # These don't need their own unique URLs
    icon: notification_important
    fields:
      - name: start_date
        type: datetime
      - name: end_date
        type: datetime
