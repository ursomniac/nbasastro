# PROJECT PLAN: NBAS ASTRO (V0.17.0 PREP)
**DATE:** 2026-04-26
**STATUS:** ACTIVE | [V: 0.9.0 AGENT]

## 1. COMPLETED (V0.16.3 RECAP)
- [x] **Taxonomy Refactor:** Converted `/tags` from bloated cards to high-density alphabetical pills.
- [x] **Technical Nav:** Deployed CLI-style "Button-Row" jump links for tag indexing.
- [x] **Data Integrity:** Fixed article count mismatch using `len .Pages` instead of `.Count`.
- [x] **Palette Sync:** Unified styling with `01-variables.css` (Space Blue / Gold / Midnight Cyan).

## 2. PENDING TASKS (V0.17.0 INCREMENTAL)
- [ ] **Series Interconnectivity:**
    - Update `single.html` to convert static `[SERIES-NAME]` header into a functional link to the series endpoint.
    - Style: Ensure compliance with `--gold-link` and specific hover states.
- [ ] **Tag Bar Injection:**
    - Create a horizontal tag bar below the article byline.
    - Implementation: Use `#tag-name` format with `tag-pill-sm` styling.
- [ ] **Navigation Refinement:**
    - Verify "Jump-Link" offsets to ensure headers don't hide under the site header on mobile/desktop.

This should use the same styling as is done on the article "card" for consistency.

## 3. BUG - "OBJECT SPECS"

This widget is defined in the metadata.  It generates a card on the Article page
but it hard-anchored to the right just below the Title - just hanging there.
Because of this it wreaks above HAVOC with the pages, especially if there are any
nbas-images present.

WHAT I WANT.  Can we change this to a shortcode somehow?
* pull the metadata if present
* put the panel with content floating around it
* allow align left|right or if align isn't present just center it (not likely
that'll be used much, centered, but that'll handle things without failures).


## 4. TECHNICAL DEBT / BACKLOG
- [ ] **Content Modeling:** Refine `knowledgetopics` taxonomy.
- [ ] **"Next in Series" Logic:** Evaluate feasibility of a "Next Article" footer for series-driven content.

## 5. REQUIRED INPUTS (NEXT SESSION)
- `layouts/_default/single.html` (or specific newsletter layout).
- Review of existing `.byline` CSS to ensure tag bar alignment.

[CANARY: NEBULA] | [STATUS: ACTIVE] | [V: 0.9.0]
