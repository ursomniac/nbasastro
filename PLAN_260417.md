# NBAS Project Plan: April 17, 2026
**Current Version:** V0.10.1 (Stable Release Candidate)

STATUS:
1. Several issues came up in the last session that were not resolved (AI looping and hallucinating, going back and forth on the same "changes" that did not resolve the problem).  MOST of this happened when we took a very complicated single custom.css file that was in static/css and split it into several CSS files under assets/css - while MOST of the site renders properly, some elements either don't have the right CSS applied, or splitting up the files put those elements in the "wrong place" (maybe - TBD).   The AI needs to know where all the CSS elements are, spread across ~8 CSS files (that are organized).

   * Tables are "stuck" in terms of the styling - they are not obeying the styles set in the *.css files
   * Widgets also have a styling problem - should be straightforward to solve
   * Other formatting is no longer "right" - these can be addressed one at a time

2. The PLAN used yesterday didn't get updated, so the current PLAN is a mix of things that were done, things that are still in flux, and things that are still "to do".   I will try to fix what's below, to properly organize this.  Items will show DONE, IN FLUX, TODO where those identification can be made.

## 0. Changes:
*   **0.1 internal links** should match the "yellow/gold" color of the logo, or something closer to that - there's too much "gold" - IN FLUX
*   **0.2 Tags** should be added to the article "card" with links to their article listing pages - DONE
*   **0.3 Categories** should have links to their article list page; the box can stay as-is but we need a way to show it's also clickable - DONE

## 🟢 1. CRITICAL DISCOVERY UI (Tomorrow's Priority)
*   **1.1 The "Archive" Hub:** Create `content/articles/_index.md` and the associated template so `/articles/` shows the full, paginated list of everything. - DONE this is the "Discovery" page, and there is now a working modal off of the top nav.
*   **1.2 Taxonomy Navigation:** We need a "Browse by Category" or "Browse by Tag" widget in the sidebar or a dedicated page so users can actually *find* the subsets (Deep Sky, Solar System, etc.). -- DONE
*   **1.3 Search Engine Experiment:** Look into a simple client-side search (Fuse.js) so the 100+ pages are searchable without a database. - TODO

## 🟡 2. VISUAL REFINEMENT
*   **2.1 Batch 2 Feature SVGs:** Create the remaining 15 gold icons (Equipment, Tips, History, etc.) to match the `TAXONOMY_MAP.md`. - TODO
*   **2.2 Interactive Sky (Plan 3.2):** Experiment with the "Set to Adams, MA" sync button for the Virtual Sky widget. - TODO, not a primary task for this pass
*   **2.3 The "Metadata Panel":** Design a clean "Object Specs" table for the sidebar of science articles (Object Type, Magnitude, etc.). - IN PROGRESS - this is "up" but the page styling isn't right.  Adding this SEEMS to have caused the other table styling issues mentioned above.

## ⚪ 3. MUSINGS & PENDING
*   **3.1 Editorial Workflow:** Finalize the "Contributor" guide—how do others add articles without breaking the Year/Month structure? - TODO - can move to tomorrow's PLAN
*   **3.2 Branch Protection:** Move the GitHub repo to Private and set up the 'dev' branch for staging. - TODO - move to tomorrow's PLAN

## 4. pre-V1.0.0 Checklist/Concerns
These have to be addressed, at least with a "how to get this done", if not solved:

*   **4.1 Galleries** - TODO - should be available to article pages, with options for:
       1. slideshow
       2. grid
       3. ??? aren't there other design ways to display a gallery 
*   **4.2 Utility Page Completion** - TODO - need to create the final list of pages
*   **4.4 Documentation** - TODO - deferred until the site is more "ready": for members to go through:
Somehow this needs to be hidden from the public but links that members can get to
       1. How to set things up locally (Windows, Mac) to be able to preview content
       2. How to submit articles for editorial review
          * Ideally this should be scripted somehow - we want to avoid members 
            needing to be "git experts"
       3. How to take an article template and make changes for an article
          * Is there a way to select a template and just "fill in the blanks"?
       4. Basically a step-by-step content creation flow
*   **4.5 Github / Security** - DEFERRED, TODO - once the site is closer to V1.0 release
       1. We need to avoid things going live without review (for content at least)
*   **4.6 Social Media Integration** - TODO - defer until JUST BEFORE the V1.0 release
       1. Articles should have a metadata setting for "create a Facebook posting to our group" - this would be triggered ONLY after editorial review AND if the publish date is reached.       2. We need a link on the site pointing to our FB group


